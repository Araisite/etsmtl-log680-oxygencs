apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: "/oxygencs-grp1-eq13"
            backend:
              service:
                name: oxygen-ingress
                port:
                  number: 8080
